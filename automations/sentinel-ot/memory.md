# Sentinel OT memory

- 2026-03-27T04:06:31.7585253+00:00: Expanded explorer/runtime regression coverage and fixed two contract gaps.
- Added transfer-progress coverage for `fileDownload` and `fileUpload`, then updated `/C:/Dev/overlayterm/src/runtime/explorerBackend.ts` so upload/download tasks report percent, failure, and completion like other byte-tracked transfers.
- Added theme-engine fallback coverage and fixed `/C:/Dev/overlayterm/src/runtime/themeEngineBackend.ts` so stale `defaultRenderStyleId` values fall back to the first available render style instead of `null`.
- Verified targeted Sentinel suites: `npx vitest run src/test/explorerBackend.bindings.test.ts src/test/themeEngineBackend.test.ts src/test/themePackages.test.ts src/test/useFolderPluginRuntime.test.tsx src/test/searchTelemetry.test.ts src/test/fileExplorer.searchTelemetry.test.tsx` and `cargo test -p yazi-specta --lib`.
- Durable decision: keep watcher bindings marked planned until watcher event DTOs ship; scheduler manifest bridging remains the stable source of transfer-progress contract coverage.
- 2026-03-27T05:04:40+00:00: Revalidated the expanded Yazi/runtime coverage already present in the worktree with no fresh regressions.
- Verified `npx vitest run src/test/explorerBackend.bindings.test.ts src/test/themeEngineBackend.test.ts src/test/themePackages.test.ts src/test/useFolderPluginRuntime.test.tsx src/test/fileExplorer.searchTelemetry.test.tsx src/test/fileExplorerSearchScope.test.ts src/test/searchTelemetry.test.ts`, `npx vitest run src/test/settingsStore.test.ts src/test/settingsPage.behavior.test.tsx src/test/appearance.test.ts`, `npx vitest run src/test/explorerStore.test.ts src/test/explorerRailState.test.ts src/test/explorerRail.performance.test.ts src/test/explorerBackend.bindings.test.ts`, `cargo test -p yazi-specta --lib`, and `cargo test -p overlay-theme --lib`.
- Durable decision: current regression buckets cover transfer progress, scheduler manifest stability, recursive search scope isolation, plugin watcher debounce/polling cleanup, settings/layout migration behavior, and theme compatibility/render-style fallback. The recurring `--localstorage-file` Vitest warning is non-blocking noise until it correlates with a failing suite.
