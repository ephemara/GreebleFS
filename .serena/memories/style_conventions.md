Favor explicit, LLM-readable structure and self-explanatory names. Prefer data-driven systems over inline constants when routing paths, toggles, manifests, pack ids, or extension points. Preserve the product identity as a premium, transformable desktop shell with the explorer as flagship workspace. Respect repo layering: Rust/native truth first, Specta bindings second, TS runtime/store orchestration third, React rendering last. Avoid raw `invoke()` strings in components when typed runtime helpers exist. Do not hand-edit generated `src/generated/tauri.ts`. For Settings, use the catalog/store/content-root seams rather than ad hoc JSX lists. For theme/UI work, route through theme bundles, appearance/theme config, CSS vars, icon theme seams, and interaction motion rather than hardcoded local styling.