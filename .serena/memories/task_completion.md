Prefer targeted validation for the specific slice touched. Typical completion steps: regenerate bindings with `cargo run --manifest-path src-tauri/Cargo.toml --bin export-bindings` after Rust/Specta changes; run targeted Vitest or Rust tests for the edited area; use broader commands like `bun run test:unit`, `bun run test:browser`, `bun run test:rust`, or `bun run build` only when justified. State clearly when repo-wide typecheck/test failures are unrelated. If a task changes architecture or spans several files, update `memory.md`; update `ARCHITECTURE.md` when the stable architecture or key seams materially change.