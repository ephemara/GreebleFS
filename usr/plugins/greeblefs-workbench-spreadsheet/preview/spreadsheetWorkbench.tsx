import { definePreviewLane } from 'overlayterm-plugin';
import { SpreadsheetWorkbenchPreviewAdapter } from 'greeblefs-workbenches';

export default definePreviewLane({
  component: SpreadsheetWorkbenchPreviewAdapter,
});
