import { definePreviewLane } from 'overlayterm-plugin';
import { ModelWorkbenchPreviewAdapter } from 'greeblefs-workbenches';

export default definePreviewLane({
  component: ModelWorkbenchPreviewAdapter,
});
