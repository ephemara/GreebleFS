import { definePreviewLane } from 'overlayterm-plugin';
import { TextWorkbenchPreviewAdapter } from 'greeblefs-workbenches';

export default definePreviewLane({
  component: TextWorkbenchPreviewAdapter,
});
