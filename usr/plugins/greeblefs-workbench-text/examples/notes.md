# Text workbench fixture

This Markdown file is shipped with the first-party Monaco/text workbench so the Plugins panel can exercise both preview and edit chrome.

```ts
export const message = 'GreebleFS workbench plugins are swappable';
```
