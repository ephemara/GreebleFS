import { definePreviewLane } from 'overlayterm-plugin';
import { ShaderWorkbenchPreviewAdapter } from 'greeblefs-workbenches';

export default definePreviewLane({
  component: ShaderWorkbenchPreviewAdapter,
});
