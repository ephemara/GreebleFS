import { definePreviewLane } from 'overlayterm-plugin';
import { ArchiveWorkbenchPreviewAdapter } from 'greeblefs-workbenches';

export default definePreviewLane({
  component: ArchiveWorkbenchPreviewAdapter,
});
