import { definePreviewLane } from 'overlayterm-plugin';
import { VideoWorkbenchPreviewAdapter } from 'greeblefs-workbenches';

export default definePreviewLane({
  component: VideoWorkbenchPreviewAdapter,
});
