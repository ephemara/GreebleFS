version = 1
id = "compression-ratio"
name = "Compression Ratio"
description = "Calculates and reports compression ratios for archive files."
icon = "FileZip"
tags = ["archive", "compression", "analysis"]
contexts = ["entry", "multi-select"]
appliesTo = "file"

[selection]
minCount = 1
allowFiles = true
allowDirectories = false

[execution]
runner = "interpreter"
entry = "python/compression_ratio.py"

[presentation]
outputTarget = "task-center"
