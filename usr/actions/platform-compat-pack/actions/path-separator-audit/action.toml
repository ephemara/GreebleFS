version = 1
id = "path-separator-audit"
name = "Path Separator Audit"
description = "Checks for mixed path separators and reports platform compatibility issues."
icon = "GitBranch"
tags = ["paths", "separators", "compatibility", "windows", "unix"]
contexts = ["entry", "multi-select"]
appliesTo = "any"

[selection]
minCount = 1
allowFiles = true
allowDirectories = true

[execution]
runner = "interpreter"
entry = "python/path_separator_audit.py"

[presentation]
outputTarget = "task-center"
