# Kain WASM Lane

The WASM lane is for Kain-authored portable artifacts that GreebleFS can consume through existing `wasm-panel` and `wasm-worker` runtime surfaces.

Keep the build contract runtime-owned: Kain authors and compiles the artifact, while GreebleFS validates, caches, and mounts it through the runtime pipeline.
