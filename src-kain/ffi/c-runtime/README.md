# Kain C Runtime

This lane is for native C ABI, DLL, SDK, and legacy-code integration.

Use it for experiments and domain SDKs that are easier to orchestrate from Kain before deciding whether the hardened boundary should graduate into Rust.
